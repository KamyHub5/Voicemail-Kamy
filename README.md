# Vonage Voicemail Project

- `index.js` → Cloud Runtime NCCO script
- `server.js` → handles recording callbacks
- Update `eventUrl` in `index.js` to point to your public callback URL
- Deploy workspace in Vonage Dashboard linked to this repo
